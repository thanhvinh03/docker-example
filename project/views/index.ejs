<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách sản phẩm</title>
    <!-- Thêm Tailwind CSS từ CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-800">
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold text-center mb-6">Danh sách sản phẩm</h1>
        <ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            <% products.forEach(product=> { %>
                <li class="bg-white shadow-md rounded-lg overflow-hidden">
                    <img src="<%= product.image %>" alt="<%= product.name %>" class="w-full h-48 object-cover">
                    <div class="p-4">
                        <h2 class="text-lg font-semibold">
                            <%= product.name %>
                        </h2>
                        <p class="text-gray-700">Giá: <%= product.price %>
                        </p>
                        <a href="/product/<%= product.id %>"
                            class="mt-2 inline-block text-blue-500 hover:text-blue-700">Xem chi tiết</a>
                    </div>
                </li>
                <% }) %>
        </ul>
    </div>
</body>

</html>